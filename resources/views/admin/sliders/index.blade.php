@extends('admin.layouts.app')

@section('title', 'Slider đa ngôn ngữ')
@section('page_title', 'Quản lý slider đa ngôn ngữ')

@section('content')
<div class="card"><div class="card-header d-flex flex-wrap gap-2 justify-content-between align-items-center"><div><h5 class="card-title mb-1">Danh sách slider</h5><p class="text-muted mb-0">Mỗi ngôn ngữ có tập slider hero riêng.</p></div><a href="{{ route('admin.sliders.create', ['locale' => $locale]) }}" class="btn btn-primary">Thêm slider</a></div><div class="card-body"><ul class="nav nav-pills mb-4">@foreach($locales as $code => $name)<li class="nav-item"><a class="nav-link {{ $locale === $code ? 'active' : '' }}" href="{{ route('admin.sliders.index', ['locale' => $code]) }}">{{ $name }}</a></li>@endforeach</ul><div class="table-responsive"><table class="table table-hover align-middle"><thead class="table-light"><tr><th>Ảnh</th><th>Thứ tự</th><th>Nội dung</th><th>CTA</th><th>Trạng thái</th><th class="text-end">Thao tác</th></tr></thead><tbody>@forelse($sliders as $slider)<tr><td><img src="{{ asset($slider->image_path) }}" alt="" class="rounded" style="height:54px;width:96px;object-fit:cover"></td><td>{{ $slider->sort_order }}</td><td><div class="fw-medium">{{ $slider->title }}</div><small class="text-muted">{{ Str::limit($slider->description, 80) }}</small></td><td>{{ $slider->button_label ?: '—' }}</td><td><span class="badge {{ $slider->is_active ? 'bg-success' : 'bg-secondary' }}">{{ $slider->is_active ? 'Hiển thị' : 'Đang ẩn' }}</span></td><td class="text-end"><a href="{{ route('admin.sliders.edit', $slider) }}" class="btn btn-sm btn-soft-warning">Sửa</a><form class="d-inline" method="POST" action="{{ route('admin.sliders.destroy', $slider) }}">@csrf @method('DELETE')<button type="submit" class="btn btn-sm btn-soft-danger" onclick="return confirm('Bạn có chắc muốn xóa slider này?')">Xóa</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Chưa có slider cho ngôn ngữ này.</td></tr>@endforelse</tbody></table></div>{{ $sliders->links('pagination::bootstrap-4') }}</div></div>
@endsection
